<%= link_to(image_tag(@recipe.image, :alt => "recipe photo", :title => @recipe.label), @recipe.url) %>
<h2><%= @recipe.label %></h2>

<h3>Ingredients</h3>
<% @recipe.ingredientLines.each do |recipe| %>
    <p><%= recipe %></p>
<% end %>
