<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="row">
      <section class="column small-7">
        <h1><%= link_to "Edamam Api Muncher", root_path %></h1>
      </section>
      <section class="search-form column small-5 search-form">
        <%= form_tag root_path, method: :get do %>
          <ul>
            <li><%= label_tag(:searchterm, "") %></li>
            <li><%= text_field_tag(:searchterm) %></li>
            <li><%= hidden_field_tag :page, 1 %></li>
            <li><%= submit_tag("Search", class: "search-button") %></li>
          </ul>
        <% end %>
      </section>
    </header>

    <%= yield %>

    <footer>
      <p> &copy; <%= Time.now.year %> <%= link_to image_tag('edamam.png', alt: "Edamam logo"), 'http://developer.edamam.com' %></p>
    </footer>
  </body>
</html>
